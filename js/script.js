'use strict'

const number1 = parseFloat(prompt("Введите число:"));

const number2 = parseFloat(prompt("Введите число:"));

const number3 = parseFloat(prompt("Введите число:"));

const average = (number1 + number2 + number3) / 3;

alert("Среднее значение: " + average);